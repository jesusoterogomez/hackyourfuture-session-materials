{
  "name": "paws-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "sync": "wrangler d1 execute paws-db --remote --file=src/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
